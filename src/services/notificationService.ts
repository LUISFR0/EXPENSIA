import { Alert } from 'react-native';

export function configureNotifications() {
  // Notifications will be configured in a future update
}

export function scheduleDailyReminder() {
  Alert.alert('Proximamente', 'Los recordatorios estaran disponibles en la proxima version.');
}
